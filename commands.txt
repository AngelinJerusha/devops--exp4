git init
git add .
git commit -m "exp4"
git remote add origin https://github.com/AngelinJerusha/devops--exp4.git
git branch -M main
git push -u origin main


now create a new folder 
github/workflows/ci-cd.yml

git add .
git commit -m "added ci-cd"
git push

after adding the secrets in github>variables>actions>repo secret

trigger the pipeline
git add .
git commit -m "Trigger CI test"
git push

